services:
  - type: web
    name: telein-webhook
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn endvan:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0 